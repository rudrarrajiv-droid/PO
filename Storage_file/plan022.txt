# PACKWELL ERP
# PHASE 22 — MR / RM BASIC CRUD FOUNDATION

PHASE 1 TO PHASE 21 HAVE BEEN COMPLETED AND APPROVED.

NOW EXECUTE ONLY PHASE 22.

============================================================
🚨 EXTREME DATA SAFETY
============================================================

THE EXISTING APPLICATION IS WORKING.

THIS PHASE MUST ONLY IMPLEMENT BASIC MR/RM RECORD MANAGEMENT
USING THE FIELD STRUCTURE APPROVED IN PHASE 20 AND IMPLEMENTED
IN PHASE 21.

DO NOT MODIFY ANY EXISTING MODULE.

DO NOT DELETE EXISTING DATA.

DO NOT MODIFY EXISTING:

- PO DATA
- PO IN
- PO OUT
- PO HISTORY
- CUSTOMER SUMMARY
- DASHBOARD
- JOB CARD
- MASTER DATA
- REEL INVENTORY
- FINISH GOODS
- TRACKER
- FREIGHT
- LOGIN/AUTH

DO NOT DEPLOY.

============================================================
PHASE 22 OBJECTIVE
============================================================

Implement ONLY BASIC CRUD for:

1. MR
2. RM

CRUD means:

CREATE

READ

UPDATE

DELETE

BUT DELETE MUST HAVE A SAFE CONFIRMATION.

IMPORTANT:

DO NOT IMPLEMENT MR/RM BUSINESS LOGIC YET.

DO NOT IMPLEMENT CALCULATIONS.

DO NOT IMPLEMENT AUTOMATION.

DO NOT IMPLEMENT STOCK MOVEMENT.

DO NOT IMPLEMENT APPROVAL WORKFLOW.

============================================================
STEP 1 — CREATE MR RECORD
============================================================

Use ONLY the confirmed MR fields from Phase 20/21.

User should be able to:

OPEN MR PAGE

→ ADD NEW MR

→ ENTER CONFIRMED FIELDS

→ SAVE

After successful save:

show the new MR record in the MR table.

Do not create any unrelated records.

============================================================
STEP 2 — CREATE RM RECORD
============================================================

Use ONLY the confirmed RM fields from Phase 20/21.

User should be able to:

OPEN RM PAGE

→ ADD NEW RM

→ ENTER CONFIRMED FIELDS

→ SAVE

After successful save:

show the new RM record in the RM table.

Do not create any unrelated records.

============================================================
STEP 3 — READ / LIST
============================================================

MR page must display saved MR records.

RM page must display saved RM records.

Use:

- Existing table component/design
- Existing pagination if available
- Existing loading state
- Existing empty state
- Existing error handling

Do not create unnecessary new UI patterns.

============================================================
STEP 4 — EDIT MR
============================================================

Provide an EDIT action for MR records.

When editing:

Load the existing MR record.

Allow modification ONLY of confirmed editable fields.

Do NOT modify system metadata unless the existing application already
handles timestamps automatically.

Do NOT change unrelated records.

============================================================
STEP 5 — EDIT RM
============================================================

Provide an EDIT action for RM records.

When editing:

Load the existing RM record.

Allow modification ONLY of confirmed editable fields.

Do NOT modify unrelated records.

============================================================
STEP 6 — DELETE MR
============================================================

Provide a DELETE action only if the existing application architecture
supports safe deletion.

Before deletion show a clear confirmation:

"Are you sure you want to delete this MR record?"

Buttons:

CANCEL

DELETE

If CANCEL:

NO DATABASE CHANGE.

If DELETE:

delete ONLY the selected MR record.

Do not delete related records.

============================================================
STEP 7 — DELETE RM
============================================================

Same safe process for RM.

Confirmation required.

Delete ONLY the selected RM record.

Do not delete related records.

============================================================
STEP 8 — DUPLICATE SUBMISSION PROTECTION
============================================================

Prevent accidental duplicate creation caused by:

- Double-clicking Save
- Repeated form submission
- Slow network
- Refresh during submission

Disable/lock the submit action while the request is processing.

Do not create duplicate records.

============================================================
STEP 9 — VALIDATION
============================================================

Use ONLY the validation rules confirmed in Phase 20/21.

Do not invent new business validation.

Required fields must be checked before save.

Invalid data must not be saved.

Show clear field-level errors.

============================================================
STEP 10 — LOADING STATES
============================================================

During:

CREATE

UPDATE

DELETE

READ

show appropriate loading state.

Prevent duplicate actions while a request is running.

============================================================
STEP 11 — ERROR HANDLING
============================================================

If database operation fails:

DO NOT show false success.

Show a clear error message.

Do not partially modify unrelated data.

============================================================
STEP 12 — SUCCESS FEEDBACK
============================================================

After successful operations show a compact confirmation:

MR created successfully.

MR updated successfully.

MR deleted successfully.

RM created successfully.

RM updated successfully.

RM deleted successfully.

Use the existing application's notification/toast system.

============================================================
STEP 13 — NO BUSINESS CALCULATIONS
============================================================

THIS PHASE MUST NOT IMPLEMENT:

- MR formulas
- RM formulas
- MR totals
- RM totals
- Automatic quantity calculation
- Automatic rate calculation
- Automatic value calculation
- Stock calculation
- Inventory movement
- Approval status automation
- PO linkage
- Job Card linkage
- Finish Goods linkage
- Reel Inventory linkage

Those belong to future approved phases.

============================================================
STEP 14 — DATABASE SAFETY
============================================================

Use the existing application's database architecture.

Do NOT:

- Create duplicate collections
- Rename existing collections
- Reset collections
- Migrate existing data
- Modify unrelated documents
- Create fake records

If MR/RM collection structure already exists from an earlier phase:

reuse it.

If it does not exist and Phase 21 explicitly established the required
structure:

use ONLY that structure.

============================================================
STEP 15 — EXISTING DATA PROTECTION TEST
============================================================

Before testing, identify representative existing records from:

PO

Job Card

Master Data

Reel Inventory

Finish Goods

Tracker

Freight

Then perform MR/RM CRUD tests.

After testing verify those records remain unchanged.

============================================================
STEP 16 — MR CRUD TEST
============================================================

Test:

CREATE MR

READ MR

EDIT MR

READ UPDATED MR

DELETE MR

Verify:

Only the selected MR record changes.

No unrelated data changes.

============================================================
STEP 17 — RM CRUD TEST
============================================================

Test:

CREATE RM

READ RM

EDIT RM

READ UPDATED RM

DELETE RM

Verify:

Only the selected RM record changes.

No unrelated data changes.

============================================================
STEP 18 — REFRESH TEST
============================================================

After creating MR/RM:

refresh browser.

Expected:

Records remain available.

After editing:

refresh browser.

Expected:

Updated values remain.

After deleting:

refresh browser.

Expected:

Deleted record is no longer shown.
